import React from 'react'
import {MetaData} from "next"

export const generateMetaTag=({params})=>{
  return{
    title:`product`,
    
  }
}

const Product = () => {
  return (
    <div>Product</div>
  )
}

export default Product